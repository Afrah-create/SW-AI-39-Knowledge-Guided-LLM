{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "bash -c 'gunicorn app.main:app --bind 0.0.0.0:${PORT} --workers 1 --threads 2 --timeout 300 --preload'",
    "restartPolicyType": "ON_FAILURE"
  }
}

